
Readme · MD
Copy

# 🔐 Flutter Authentication App

Мобильное приложение для аутентификации с верификацией через email, использованием JWT и Refresh Token.

## 📱 Возможности

- ✅ **Аутентификация через Email** - Вход через код подтверждения на почту
- ✅ **JWT & Refresh Token** - Безопасное управление токенами
- ✅ **Автообновление токенов** - Автоматическое обновление JWT (срок действия 1 час)
- ✅ **Постоянная авторизация** - Пользователь остается авторизованным после перезапуска приложения
- ✅ **Валидация Email** - Проверка формата email в реальном времени
- ✅ **Безопасное хранилище** - Токены хранятся в FlutterSecureStorage
- ✅ **Современный UI/UX** - Интерфейс на Material Design 3
- ✅ **Обработка ошибок** - Понятные сообщения об ошибках

## 🚀 Начало работы

### Требования

- Flutter SDK (>=3.0.0)
- Dart SDK (>=3.0.0)

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/flutter-auth-app.git
cd flutter-auth-app
```

2. Установите зависимости:
```bash
flutter pub get
```

3. Запустите приложение:
```bash
flutter run
```

## 📦 Зависимости

```yaml
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0                          # HTTP клиент
  flutter_secure_storage: ^9.0.0      # Безопасное хранилище токенов
```

## 🏗️ Структура проекта

```
lib/
├── main.dart                 # Точка входа приложения
├── screens/
│   ├── splash_screen.dart   # Загрузка и проверка авторизации
│   ├── login_page.dart      # Экран ввода email
│   ├── confirm_page.dart    # Экран ввода кода подтверждения
│   └── home_screen.dart     # Экран авторизованного пользователя
├── services/
│   └── auth_service.dart    # Логика аутентификации
├── storage/
│   └── token_storage.dart   # Управление токенами
└── widgets/
    └── auth_textfield.dart  # Кастомный виджет ввода
```

## 🔑 Процесс аутентификации

```
1. Пользователь вводит email
   ↓
2. Сервер отправляет код подтверждения
   ↓
3. Пользователь вводит код
   ↓
4. Сервер возвращает JWT + Refresh Token
   ↓
5. Токены сохраняются безопасно
   ↓
6. Переход на главный экран
```

## 🔄 Управление токенами

- **JWT (Access Token)**: 
  - Срок действия: 1 час
  - Используется для API запросов
  - Автоматически обновляется при истечении

- **Refresh Token (RT)**:
  - Одноразовый
  - Без ограничения срока действия
  - Используется для получения новой пары JWT + RT

### Логика автообновления

```
1. Приложение делает запрос с JWT
   ↓
2. JWT истёк? (ошибка 403)
   ↓
3. Используем Refresh Token для получения нового JWT
   ↓
4. Сохраняем новые токены
   ↓
5. Повторяем исходный запрос
```

## 🌐 API Endpoints

Base URL: `https://d5dsstfjsletfcftjn3b.apigw.yandexcloud.net`

### 1. Отправка кода подтверждения
```http
POST /login
Content-Type: application/json

{
  "email": "user@example.com"
}
```

### 2. Подтверждение кода и получение токенов
```http
POST /confirm_code
Content-Type: application/json

{
  "email": "user@example.com",
  "code": 123456
}
```

**Response:**
```json
{
  "jwt": "eyJhbGci...",
  "refresh_token": "ZXlKaG..."
}
```

### 3. Обновление токенов
```http
POST /refresh_token
Content-Type: application/json

{
  "token": "refresh_token_here"
}
```

**Response:**
```json
{
  "jwt": "new_jwt_token",
  "refresh_token": "new_refresh_token"
}
```

### 4. Получение User ID
```http
GET /auth
Auth: Bearer 
```

**Response:**
```json
{
  "user_id": "c13cf334-18be-432e-9444-1fda91f6c418"
}
```

## 📸 Скриншоты

### Экран входа
- Ввод email с валидацией
- Кнопка "Получить код"
- Современный дизайн

### Экран подтверждения
- Ввод 6-значного кода
- Крупный текст по центру
- Опция "Отправить код повторно"

### Главный экран
- Сообщение "Авторизован"
- Отображение User ID
- Кнопка выхода

## 🧪 Тестирование

### Сценарии для проверки:

#### ✅ Базовая авторизация
1. Запустите приложение
2. Введите корректный email
3. Нажмите "Получить код"
4. Проверьте почту (включая спам)
5. Введите полученный код
6. Проверьте, что открылся экран с User ID

#### ✅ Валидация
- Введите неверный email (без @) → должна показаться ошибка
- Введите неверный код → должна показаться ошибка "Неверный код"

#### ✅ Истечение кода
- Подождите 10+ минут
- Введите старый код → должно показать "Код истёк"
- Нажмите "Отправить код повторно"
- Введите новый код → должно работать

#### ✅ Постоянная авторизация
1. Авторизуйтесь
2. Полностью закройте приложение
3. Откройте снова
4. **Результат:** Должен сразу открыться экран с User ID (без повторного входа)

#### ✅ Выход из системы
1. На главном экране нажмите "Выйти"
2. Должен открыться экран входа
3. Закройте и откройте приложение
4. **Результат:** Должен показаться экран входа (не главный экран)

#### ✅ Автообновление токенов
1. Авторизуйтесь
2. Подождите 1+ час (или измените системное время)
3. Закройте и откройте приложение
4. **Результат:** JWT должен автоматически обновиться через Refresh Token

## 🔧 Возможные проблемы

### Ошибка 403 Forbidden

**Проблема:** Неверный код или код истёк

**Решение:**
- Проверьте правильность ввода кода
- Запросите новый код через "Отправить код повторно"
- Убедитесь, что email совпадает

### Ошибка 400 Bad Request

**Проблема:** Неверный формат данных

**Решение:**
- Код должен состоять только из цифр
- Не используйте пробелы

### "No JWT found" при перезапуске

**Проблема:** Токены не сохранились

**Решение:**
- Убедитесь, что после `confirmCode()` вызывается:
```dart
await authService.tokenStorage.saveTokens(result);
```

### Приложение не запускается

**Решение:**
```bash
flutter clean
flutter pub get
flutter run
```

## 📝 Ключевые моменты реализации

### 1. Безопасное хранилище
```dart
final storage = const FlutterSecureStorage();
```
- Токены хранятся в зашифрованном виде
- Недоступны другим приложениям

### 2. Автоматическое обновление JWT
```dart
if (e.response?.statusCode == 403) {
  final newTokens = await refreshTokens(rt);
  await tokenStorage.saveTokens(newTokens);
}
```
- При истечении JWT (403) автоматически обновляется
- Пользователь не замечает обновления

### 3. Проверка при запуске
```dart
final userId = await authService.getValidUserId();
if (userId != null) {
  // Авторизован
} else {
  // Не авторизован
}
```
- SplashScreen проверяет токены
- Автоматический переход на нужный экран

## 👨‍💻 Автор

**Ваше имя**
- GitHub: [@yourusername](https://github.com/yourusername)
- Email: your.email@example.com

## 📄 Лицензия

Этот проект создан в рамках учебного задания.

## 🙏 Благодарности

- [Dio](https://pub.dev/packages/dio) - HTTP клиент
- [FlutterSecureStorage](https://pub.dev/packages/flutter_secure_storage) - Безопасное хранилище
- [Flutter](https://flutter.dev/) - UI фреймворк

---

⭐ Если проект был полезен, поставьте звезду на GitHub! 
